import 'package:flutter/material.dart';

class ChangelleScreen extends StatelessWidget {
  const ChangelleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}